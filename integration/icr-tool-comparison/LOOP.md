# Comparative reconnaissance mission

**COMPARISON_STATUS = SATURATED.** Started and completed assessment: 2026-09-10.

## Purpose and authority

Compare authoritative repository structure, workflows, domain calculations, data/persistence, evidence, validation, runtime and integration options. Produce an integration decision record, not implementation. Repository evidence overrides historical navigation documentation and conversation summaries.

## Exact inspected state

- Benchmark: `Lukemendels/ICR-12-15-Benchmark`, `~/Projects/ICR-12-15-Benchmark`, branch `main`, HEAD `29af4a08f098ebd797f1449ca4fa3418afcaf10c`, origin `https://github.com/Lukemendels/ICR-12-15-Benchmark.git`; initially clean.
- Application: `Jpatel3333/icr-tool`, `~/Projects/icr-tool`, branch `main`, HEAD `e72fae4b1d2a37d7a22aeb8ab5b3555f423af79e`, origin `https://github.com/Jpatel3333/icr-tool.git`; absent initially, cloned to the explicitly authorized sibling path, then clean throughout.

No HEAD changed during inspection. The closeout commit adds only comparison artifacts to benchmark. Its SHA belongs in Git history and the final response, not in its own contents. The machine-readable state preserves inspected baselines independently.

## Boundaries observed

Repositories remain separate. No nesting, submodule, source vendoring, Git-history merge, application refactoring, behavior change, dependency installation, new external research, frozen-evidence change or icr-tool commit/push. All durable outputs are confined to `integration/icr-tool-comparison/`; no artifacts enter application source, completed reports or frozen releases.

## Pass history and stop conditions

Pass 1 mapped structures/entry points and initial contracts. Pass 2 traced calculations, forms/exports, SQLite and comparability. Pass 3 inspected imports, validation and research tooling and executed read-only release acceptance. Pass 4 counter-checked host, source and overlay boundaries. Pass 5 reconciled the report/evidence and Git scope. Passes 4 and 5 produced no material change in architectural conclusion.

All required structural, workflow, domain, data, runtime, validation, institutional-evidence and integration coverage is complete for architectural assessment. Exact commits, overlap/conflicts, credible options, recommendation and unresolved questions are recorded. Remaining questions require product decisions or host/substantive validation, not exhaustive file inspection. All twelve mission stop conditions are satisfied: terminal assessment state is SATURATED.

## Recommendation

Keep separate repositories. Have icr-tool consume the pinned portable JSON evidence release read-only; preserve local references/assumption decisions and working drafts separately. Define shared executable model/identity contracts and consider pure-library reuse only in a separately authorized implementation loop after resolving UI/export semantics. Preserve the evidence workbench and research/report system.

## Validation and limitations

Fresh read-only SQLite integrity check and release load/12 acceptance cases passed. Existing research/workbench gate receipts were reused. No new icr-tool test/build pass is claimed: dependencies absent. Windows/WebView2, actual Anvil import and browser DOM execution remain outside this inspection's validation. Full details are in the report and evidence log.

## Durable closeout

Deliverables: `CODEBASE-COMPARISON-REPORT.md`, `evidence-log.md`, `comparison-state.json`, and this mission record. Only these four files are eligible for the final commit: `Complete icr-tool comparative architecture and integration assessment`, published to benchmark `origin/main`. Verify staging against that allowlist, leave application/evidence files unchanged, and do not poll CI after successful push. If push is blocked, preserve the local commit and report BLOCKED_ON_REMOTE_PUSH with its exact SHA and reason. No implementation follows this mission.

For future reference read the state, report and evidence log first; saturation is a completed checkpoint, not an invitation to repeat inspection.
