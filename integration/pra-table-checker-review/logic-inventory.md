# Logic inventory

Inspected all 798 lines of [the checker](../../pra-q12-15-table-checker.html) at `dc818c0a31177aed36ebd3881a559a113447bce5`. Line locations below refer to that immutable version. No automated tests are embedded or supplied for this standalone artifact; SAMPLE_TEXT is demonstration data without assertions. Fresh tests are review-only [reproduce.mjs](reproduce.mjs), with exact inputs and outputs in [execution-results.json](execution-results.json). No function is approved for literal runtime migration.

“Pure” describes isolation, not correctness. All regex/index/number operations are deterministic for the same input; their interpretation of document meaning remains heuristic. Formatting additionally depends on runtime locale. Stateful helpers can run without DOM but write a shared global array.

| Function/subsystem; lines | Purpose, input → output | Assumptions and determinism | DOM/UI dependence | Apparent correctness and fresh tests | Classification; Jay/benchmark relationship |
|---|---|---|---|---|---|
| Outer shell/CSS; 1–234 | Layout/style → rendered page | Static packaging | Entirely UI | Nested second doctype/html/head/body at 235–242 and early closing tags 321–322; browser repair not tested | PROTOTYPE_ONLY; no domain value |
| Inner UI; 235–322 | Text input, buttons, result containers | Claims Q12–15 and narrative/formula checking beyond implementation | DOM | Reviewer-aid footer is appropriate; actual scope much narrower | PROTOTYPE_ONLY; no visual comparison |
| tool-descriptor/STICKSHIFT_TOOL/generated skill; 323–344 | Paperclip portable-tool v1 metadata, project v1.0.0, launch registration | Empty prompts/capabilities; HTML_OPEN points to generated registration path | Host registration | Metadata only; no analytical skill implementation or ANVIL runtime engine here. Registration text inspected as artifact, not followed as instructions | PROTOTYPE_ONLY; do not migrate shell metadata |
| `$`, `state`; 348–350 | ID lookup; mutable checks/tables/report | Single current run | `$` requires document; state globally shared | Minimal shim executes existing code | PROTOTYPE_ONLY; Jay has typed review results |
| SAMPLE_TEXT/TABLE_CONFIG; 352–397 | Six known captions and 18 annual sample rows | LEOFA/413A only; duplicate Table 4 intentional in sample | None | Sample 50 numerical checks, 48 pass, two numerical warnings; unverified source identity | PROTOTYPE_ONLY; test/reference only, no evidence authority |
| parseNumber; 399–405 | Scalar text → finite Number or NaN | Removes all $, commas, %; Number grammar; no unit retained | Pure | ASCII negative preserved; Unicode minus, parentheses, units rejected; `1,2` becomes 12, hex accepted. Micro tests | UNSAFE_OR_INCORRECT for general input; Jay normalization richer but loses negative prose signs; benchmark needs raw/unit/sign preservation |
| roundForDisplay; 407–411 | Number + decimals → rounded Number | Math.round + absolute EPSILON; defaults 0 | Pure | -1.5 → -1, not half-away-from-zero; display rounding only, not precision policy | DUPLICATES_ICR_TOOL; prefer existing tested round.ts, declare policy |
| fmtNumber/fmtMoney/fmtValue; 413–425 | Number/kind/decimals → string | Locale-dependent; money always integer dollars; nonfinite “n/a” | Pure formatting | Collapses cents in reports; no raw numeric value in findings | DUPLICATES_ICR_TOOL; Jay separates raw ComputedCell and formatting |
| splitPipeRow/isAlignmentRow; 427–436 | Pipe text → trimmed cells; cells → separator boolean | Optional outer pipes; every cell made empty by stripping `:-` = separator | Pure heuristic | Standard alignment accepted; no escaped-pipe or span handling | REUSABLE_AFTER_REFACTOR in isolation, but duplicates Jay parser; reject literal reuse |
| extractRows; 438–458 | Text + exact caption → rows | First case-sensitive substring occurrence; skips pre-table nonpipe lines, stops at blank/nonpipe after rows; starts next table only after rows exist | Pure heuristic | Caption-only can report found; blank mid-table truncates; uppercase caption fails; first matching occurrence may be reference rather than actual caption | PROTOTYPE_ONLY; generalized parser needs line/source boundaries |
| parseTable; 460–467 | Extracted block → headers, annual rows, first Average | First row header (unused analytically); only first cells `1`,`2`,`3`,`Average`; arbitrary row order/duplicates; no width validation | Pure heuristic | Calendar years and fourth year dropped; compact average layout assumed downstream | UNSAFE_OR_INCORRECT as general parser; Jay has section/table/line structure |
| average; 469–472 | Number list → mean of finite subset, NaN if empty | Silently removes missing terms; no required horizon | Pure | [100,NaN,300] → 200; falsely reports “Years 1–3” when fewer terms | UNSAFE_OR_INCORRECT for annualization; benchmark missing ≠ zero/omission |
| addCheck/addStructure; 474–480 | Finding fields → append global check | No IDs, confidence, rule applicability, source references or numeric schema | Stateful, no DOM | Routine append works through full runs | USEFUL_CONCEPT_NOT_CODE; typed Jay Finding already stronger |
| classify/resultNote; 482–495 | Observed/expected/tolerance/decimals → severity/message | Nonfinite WARNING; equality after display rounding PASS before tolerance; otherwise abs residual ≤ tolerance WARNING, else ISSUE | Pure | All branches exercised; `100.4 vs 100.1`, tolerance 0, precision 0 → PASS. Parse failure and small residual share severity | USEFUL_CONCEPT_NOT_CODE; distinguish exact/display/rounding/incomplete using typed statuses, not present policy |
| diffText; 497–510 | Observed−expected → formatted signed residual | Returns zero whenever rounded values match; adaptive decimals otherwise | Pure formatting | 0.3 raw difference → “0”; 0.02 across rounding boundary retained; money sub-dollar fallback lacks $ | USEFUL_CONCEPT_NOT_CODE; benchmark needs signed raw residual plus display |
| addComparison; 512–520 | Numerical comparison + metadata → global finding | Converts numeric operands/residual/tolerance to strings immediately | Stateful, no DOM | Formula, observed, recalculated, residual and tolerance are useful audit dimensions; tolerance .15 prints .2 at one decimal | REUSABLE_AFTER_REFACTOR; conceptual typed result for Jay review/output validator, no literal copy |
| getSection; 522–530 | Whole text → substring | Exact start `Provide estimates of hour and cost burdens`; exact end `For collections of information whose results will be published`; otherwise full text | Pure heuristic | Phrase-slice and numbered-heading fallback exercised; does not identify individual items | PROTOTYPE_ONLY; Jay sectionizer better, also rejects unnumbered archive headings |
| buildTables; 532–541 | Text → fixed six-key object | Configuration captions hard-wired | Pure orchestration | Six empty tables when unsupported | PROTOTYPE_ONLY; use explicit applicability before parsing |
| rowNumbers; 543–545 | Table + column indices → number arrays | Positional mapping only | Pure | Used in mean controls/missing term mutation | DUPLICATES_ICR_TOOL; trivial indexing not domain seed |
| checkTablePresence; 547–557 | Text → structure findings | Exact caption includes; duplicate tokens from lines starting `Table `, first token after space | Stateful heuristic | Detects duplicate 4; narrative `Table 4 summarizes…` also counted as table caption; capitalization/number syntax brittle | USEFUL_CONCEPT_NOT_CODE; conservative duplicate/reference diagnostics for review engine |
| addParseDiagnostics; 559–563 | Tables → INFO counts/average-present | Reports parsing extent, no completeness gate | Stateful, no DOM | Correct individual row counts even when summary lies | USEFUL_CONCEPT_NOT_CODE; applicability/coverage reporting useful |
| checkInternalCalculations table1; 565–580 | Growth, share, two means → findings | Initial population 83749, sequential prior displayed row, whole-count rounding; fixed average columns 1/2 | Stateful, no DOM | Coherent control + four mutations; document-supplied baseline ignored; missing value distorts mean | USEFUL_CONCEPT_NOT_CODE; yearly projection/output reconciliation; no direct replacement of Jay annualize.ts |
| checkInternalCalculations table2; 581–589 | responses × time; displayed hours × 62.36 | Fixed columns; hours already in hours; hard-coded wage | Stateful | Two target mutations detected; changed prose wage ignored; average not checked | USEFUL_CONCEPT_NOT_CODE; model-to-output checks using bound inputs |
| checkInternalCalculations table3/table4; 590–591 | base × percent/100, rounded count | Fixed roles and columns; no share partition/range test | Stateful | One target mutation each detected | USEFUL_CONCEPT_NOT_CODE; explicit percentage relation beats blind candidate products, only if role binding valid |
| checkInternalCalculations table5; 592–597 | registrations × .0833, occurrences × .0333; displayed hours × 62.36 for both | Fixed times/wage, displayed intermediate chain, separate tolerances | Stateful | All four target mutations detected; no average or combined total | USEFUL_CONCEPT_NOT_CODE; chain audit idea; fixed constants prototype/test only |
| checkInternalCalculations federal; 598–602 | responses × time; displayed hours × 70.35 | One fixed Federal table; no roles/multi-component sum | Stateful | Two mutations detected; Pipeline unsupported | USEFUL_CONCEPT_NOT_CODE; Jay govcost.ts more general |
| compareRows/checkCrossTables; 604–616 | Four fixed edges × first three row positions → comparisons | T1 LEO→T3; T1 responses→T2; T3 occurrences→T4; T4 registrations→T5. Numeric equality at integer display precision, not label matching | Stateful | Four false issues on reordering one table; all four edge families exercised by mutations. Missing tables still generate 12 warnings | USEFUL_CONCEPT_NOT_CODE; strongest unique concept: explicit relationship audit; replace positions with identities/periods |
| checkNarrative; 618–621 | Text → at most one factor warning + one structure warning | Only literal pair 1.6265/1.6266 and two Table 4 substrings anywhere in selected text | Stateful heuristic | Unrelated substrings trigger; 1.5/1.6 contradiction ignored. No prose value or formula extraction | PROTOTYPE_ONLY; Jay MATH-004 broader, but also not semantic binding |
| runChecks; 623–641 | Textarea → reset, scope, parse, checks, render | All fixed checks run if nonempty; none has applicability guard | DOM + state | 44 complete cases; empty branch; unsupported conclusion misleading | PROTOTYPE_ONLY; typed pure reviewDocument already better architecture |
| countBy/counts; 643–656 | Checks → summary counts | Tables count titles ending “found,” including “not found”; narrative issues structurally never emitted | Stateful read | Unrelated input → 6 tables, zero parsed; numerical passes do not measure coverage | UNSAFE_OR_INCORRECT; require explicit table records/evaluation statuses |
| render/renderConclusion; 658–685 | Findings → cards/conclusion/plain report | Parse warnings counted as executed numeric checks; “no issues” language | DOM + state | Unsupported text says 12 numeric/prose checks ran with no issues and 18 warnings; empty says zero ran | UNSAFE_OR_INCORRECT conclusion; presentation prototype only |
| filterItems/setSectionVisibility/renderSections; 687–707 | Filter + checks → visible groups | “attention” excludes only pass, so INFO retained | DOM/state | Event smoke confirms INFO in warnings/issues filter | PROTOTYPE_ONLY |
| escapeHtml; 709–711 | String → HTML escaped text | Text/quoted-attribute escaping only | Pure | Escaping microcase passes; no application security certification | PURE_REUSABLE_LOGIC in isolation, but redundant with Svelte escaping; no migration recommended |
| renderAuditTable/renderFindingList/labelize/renderDetails; 713–743 | Checks/details → HTML strings | Escaped text, formatted-only values; unused `type` argument | DOM except labelize/details | Full renders execute under shim | PROTOTYPE_ONLY; audit dimensions concept already inventoried |
| buildPlainTextReport; 745–754 | State/counts → numbered report string | All findings, regardless of filter; no source IDs, input hash or baseline/version | State read, no DOM | Exact report preserved per execution case | REUSABLE_AFTER_REFACTOR conceptually; typed finding serializer only, no need for this implementation |
| copyReport; 756–765 | Last report → clipboard or prompt | Async browser clipboard; fallback on failure | Browser | Both branches executed with shim, not real permissions/clipboard | PROTOTYPE_ONLY |
| clearAll/init/startup; 767–795 | Buttons/events/reset → current UI state | Single document with expected IDs | DOM | Registered load/run/filter/clear exercised; DOMContentLoaded waiting branch static only | PROTOTYPE_ONLY |

## Complete numerical relationship register

No formula parser, eval, arbitrary expression tree, subtotal engine, component sum, weighted mean, capital annualization, benefit transformation, period conversion, Item 15 subtraction/bridge, or generic repeated-value matching exists.

| Site | Expected value | Absolute tolerance; output precision |
|---|---|---|
| T1 annual LEOs | round(prior × (1 + increase/100)); initial prior = 83749, then displayed LEOs | 1; 0 decimals |
| T1 413A responses | round(LEOs × use percentage/100) | 1; 0 |
| T1 mean LEOs / responses | mean finite parsed annual columns 2 / 4, compared to compact Average columns 1 / 2 | 1; 0 |
| T2 hours | responses × row time in hours | .15; 1 |
| T2 labor | displayed hours × 62.36 | $2; 0 |
| T3 occurrences | round(LEOs × share/100) | 1; 0 |
| T4 registrations | round(occurrences × portion/100) | 1; 0 |
| T5 registration hours / labor | registrations × .0833; displayed hours × 62.36 | .2 hours / $2; 1 / 0 |
| T5 flight hours / labor | occurrences × .0333; displayed hours × 62.36 | 1 hour / $2; 1 / 0 |
| Federal hours / labor | responses × row time; displayed hours × 70.35 | 1 hour / $2; 0 / 0 |
| Four cross-table relationships | target compared against fixed source field, first three row positions | 0; 0 |

PASS after display rounding precedes every tolerance, including zero-tolerance cross-links. Comparisons do not retain the raw residual. A successful row calculation establishes neither the wage/time assumption nor the applicability of the selected population.
