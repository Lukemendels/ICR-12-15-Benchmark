import test, {after} from 'node:test';
import assert from 'node:assert/strict';
import fs from 'node:fs';
import path from 'node:path';
import vm from 'node:vm';
import {createRequire} from 'node:module';
import {spawnSync} from 'node:child_process';
import {project,release,library,manifest,pin,read,paths,evidenceEntries,build,write,sourceValidation} from './build.mjs';
import {sha256,resolveProjectCapabilities,capabilityModelContext} from '../assets/anvil/governed-capability.mjs';
const require=createRequire(import.meta.url);
const A=require('../assets/anvil/anvil-authoring.js');
const Consumer=await import(path.join(release,'runtime/consumer.mjs'));
const state=resolveProjectCapabilities(project,library);
const context=vm.createContext({TextEncoder,TextDecoder,crypto:globalThis.crypto,Blob,Response,DecompressionStream,URL,atob,Uint8Array,console});
vm.runInContext(state.resolved[0].implementation,context);
const F=context.ICRFoundation, entries=evidenceEntries(), adapter=F.bytesAdapter(entries);
const base=await F.loadAdapter(adapter), provider=F.createQueryProvider(base), stores=F.createStores(base);
const result=[];
function check(id,fn){test(id,async()=>{try{await fn();result.push({id,status:'PASS'});}catch(e){result.push({id,status:'FAIL',error:e.message});throw e;}});}
const changed=(name,fn)=>F.bytesAdapter(entries.map(([p,b])=>[p,p===name?fn(Buffer.from(b)):b]));
const changeManifest=fn=>changed('release-manifest.json',b=>{const m=JSON.parse(b);fn(m);return Buffer.from(JSON.stringify(m));});
const reference='base:'+pin.release_id+':VER-202406-1652-001';
const overlay={schema_version:'1.0.0',base_release_id:pin.release_id,base_manifest_sha256:pin.manifest_sha256,namespace:'local:TSA',records:[{id:'local:TSA:12345678-1234-1234-1234-123456789abc',references:[reference],proposed_changes:{title:'<img src=x onerror=alert(1)>'},provenance:[],owner:'Test operator',review_status:'PROPOSED'}]};
const working={schema_version:'0.1.0',id:'project:12345678-1234-1234-1234-123456789abc',title:'Working envelope',base_release_id:pin.release_id,base_manifest_sha256:pin.manifest_sha256,references:[reference],workspace:{}};
const jsonClone=x=>JSON.parse(JSON.stringify(x));
check('correct_release_and_exact_manifest_load',()=>{assert.equal(base.manifestHash,pin.manifest_sha256);assert.equal(base.manifest.release_id,pin.release_id);assert.equal(base.validation.status,'PASS');});
check('wrong_manifest_hash_fails_closed',async()=>{await assert.rejects(F.loadAdapter(changed('release-manifest.json',b=>Buffer.concat([b,Buffer.from(' ')]))),/Manifest hash mismatch/);});
check('wrong_release_identity_fails_closed',async()=>{await assert.rejects(F.loadAdapter(changeManifest(m=>m.release_id='UNAPPROVED')),/Manifest hash mismatch/);const a=changeManifest(m=>m.release_id='UNAPPROVED');await assert.rejects(Consumer.loadRelease(p=>a.readBytes(p)),/Unsupported release identity/);});
check('incompatible_schema_and_version_fail_closed',async()=>{for(const key of ['schema_version','version']){const a=changeManifest(m=>m[key]='2.0.0');await assert.rejects(F.loadAdapter(a),/Manifest hash mismatch/);await assert.rejects(Consumer.loadRelease(p=>a.readBytes(p)),/Unsupported release identity/);}});
check('missing_artifact_fails_closed',async()=>{await assert.rejects(F.loadAdapter(F.bytesAdapter(entries.filter(([p])=>p!=='tsa/nodes.json'))),/Missing release artifact/);});
check('tampered_artifact_fails_closed',async()=>{await assert.rejects(F.loadAdapter(changed('tsa/portfolio.json',b=>Buffer.concat([b,Buffer.from(' ')]))),/Integrity mismatch/);});
check('unknown_file_and_outside_freeze_state_fail_closed',async()=>{for(const p of ['surprise.json','loop-5/freeze-state.json'])await assert.rejects(F.loadAdapter(F.bytesAdapter([...entries,[p,Buffer.from('{}')]])),/Unexpected\/missing files/);});
check('administrative_boundary_only_two_optional_paths',async()=>{const bare=F.bytesAdapter(entries.filter(([p])=>!['FREEZE-RECEIPT.json','validation/final-acceptance-gate.json'].includes(p)));assert.equal((await F.loadAdapter(bare)).manifestHash,pin.manifest_sha256);});
check('unsafe_duplicate_paths_and_missing_listing_rejected',async()=>{assert.throws(()=>F.bytesAdapter([['../escape',new Uint8Array()]]),/Unsafe/);assert.throws(()=>F.bytesAdapter([...entries,entries[0]]),/Duplicate/);await assert.rejects(F.loadAdapter({readBytes:adapter.readBytes}),/actual listPaths/);});
check('byte_adapter_defensive_copies',async()=>{const bytes=await adapter.readBytes('release-manifest.json');bytes[0]=0;assert.equal((await adapter.readBytes('release-manifest.json'))[0],123);});
check('file_and_host_adapters_match',async()=>{const files=entries.map(([p,b])=>({name:p.split('/').at(-1),webkitRelativePath:'v1.0.0/'+p,arrayBuffer:async()=>b.buffer.slice(b.byteOffset,b.byteOffset+b.byteLength)}));assert.equal((await F.loadAdapter(F.fileAdapter(files))).manifestHash,base.manifestHash);assert.throws(()=>F.fileAdapter([{name:'release-manifest.json'}]),/release directory/);});
check('bundled_payload_loads_byte_identically',async()=>{const bundled=await F.bundledAdapter();assert.deepEqual([...await bundled.listPaths()].sort(),entries.map(([p])=>p).sort());for(const [p,b] of entries)assert.equal(sha256(await bundled.readBytes(p)),sha256(b),p);assert.equal((await F.loadAdapter(bundled)).manifestHash,pin.manifest_sha256);});
check('frozen_loop_5_1_retrieval_fixtures',()=>{const report=Consumer.acceptance(base.files);assert.equal(report.status,'PASS');assert.equal(report.tests.length,12);});
check('enumerate_90_select_version_activities_sources_findings',()=>{assert.equal(provider.portfolio().length,90);const p=provider.selectPackage('202406-1652-001');assert.equal(p.ref,'202406-1652-001');const acts=provider.activities(p.ref,{limit:100});assert.ok(acts.total>0);assert.ok(acts.records.every(x=>x.record.type==='ACTIVITY'));const source=acts.records.flatMap(x=>[...provider.sourcesFor(x.record)])[0];assert.match(source.url,/^https?:/);assert.equal(provider.source(source.id),source);const f=provider.findings(p.ref);assert.ok(f.total>0);assert.equal(base.files['tsa/within-icr-findings.json'].find(x=>x.id==='FIND-CYBER-COST').id,'FIND-CYBER-COST');assert.throws(()=>provider.selectPackage('unknown'),/Unknown/);});
check('bounded_exact_paging_and_unknown_operators',()=>{const p1=provider.activities('202406-1652-001',{limit:1});assert.ok(p1.next);const p2=provider.activities('202406-1652-001',{limit:1,after:p1.next});assert.notEqual(p1.records[0].key,p2.records[0].key);assert.equal(provider.query({filters:{package:'unknown'}}).total,0);for(const q of [{limit:101},{limit:0},{sql:'SELECT *'},{filters:{unknown:'x'}},{filters:{package:{$eq:'x'}}}])assert.throws(()=>provider.query(q));});
check('provenance_hydration_and_typed_neighbors',()=>{const r=provider.activities('202406-1652-001').records.find(x=>x.record.provenance_refs.length).record;const h=provider.hydrate(r);assert.ok(h.provenance.length);assert.ok(Object.isFrozen(h.provenance[0]));assert.ok(provider.sourceLinks(r).every(x=>/^https?:/.test(x.url)));const adjacency=base.files['indexes/adjacency.json'];const id=Object.keys(adjacency)[0],type=Object.keys(adjacency[id])[0];assert.equal(provider.neighbors(id,type),adjacency[id][type]);});
check('public_base_recursively_immutable',()=>{function visit(v){if(v&&typeof v==='object'){assert.ok(Object.isFrozen(v));Object.values(v).forEach(visit);}}visit(base);const r=provider.selectPackage('202406-1652-001');assert.throws(()=>{r.title='corrupted';});assert.throws(()=>{base.files['tsa/nodes.json'][0].data.injected=true;});assert.throws(()=>{base.files['tsa/nodes.json']=[];});});
check('forged_base_rejected_by_provider_and_stores',()=>{const forged=Object.freeze({manifestHash:pin.manifest_sha256,files:base.files});assert.throws(()=>F.createQueryProvider(forged),/Verified immutable/);assert.throws(()=>F.createStores(forged),/Verified immutable/);});
check('independent_overlay_project_import_export_and_no_base_replacement',()=>{const before=JSON.stringify(base);stores.overlay.importJSON(JSON.stringify(overlay));stores.project.importJSON(JSON.stringify(working));assert.notEqual(stores.overlay.snapshot(),stores.project.snapshot());assert.equal(stores.overlay.snapshot().records[0].review_status,'PROPOSED');assert.ok(Object.isFrozen(stores.overlay.snapshot().records[0].proposed_changes));assert.deepEqual(JSON.parse(stores.overlay.exportJSON()),overlay);assert.deepEqual(JSON.parse(stores.project.exportJSON()),working);stores.overlay.clear();assert.equal(stores.overlay.snapshot(),null);assert.equal(stores.project.snapshot().id,working.id);assert.equal(JSON.stringify(base),before);});
check('overlay_identity_collision_bad_pin_reference_and_duplicate_rejected',()=>{for(const mutate of [x=>x.records[0].id='VER-202406-1652-001',x=>x.base_manifest_sha256='0'.repeat(64),x=>x.records[0].references=['base:'+pin.release_id+':missing'],x=>x.records.push(x.records[0]),x=>x.records[0].id='local:OTHER:12345678-1234-1234-1234-123456789abc']){const c=jsonClone(overlay);mutate(c);assert.throws(()=>stores.overlay.importJSON(JSON.stringify(c)));}assert.throws(()=>stores.overlay.importJSON(stores.project.exportJSON()));});
check('project_rejects_base_identity_calculation_extra_keys_and_preserves_prior',()=>{const prior=stores.project.snapshot();for(const mutate of [x=>x.id='VER-202406-1652-001',x=>x.workspace={formula:'1+1'},x=>x.base_manifest_sha256='0'.repeat(64),x=>x.base=base.files]){const c=jsonClone(working);mutate(c);assert.throws(()=>stores.project.importJSON(JSON.stringify(c)));assert.equal(stores.project.snapshot(),prior);}assert.equal(stores.project.snapshot(),prior);});
check('stored_text_and_source_links_safe_by_construction',()=>{const document={createElement:tag=>({tag})};const payload='<img src=x onerror=alert(1)><script>globalThis.compromised=true</script>';const node=F.textNode(document,'p',payload);assert.equal(node.textContent,payload);assert.equal(node.innerHTML,undefined);assert.equal(context.compromised,undefined);for(const url of ['javascript:alert(1)','data:text/html,evil','file:///tmp/private','//host/path','https://user:password@example.com'])assert.equal(F.safeSourceURL(url),null);const link=F.sourceLink(document,{id:payload,url:'https://www.reginfo.gov/public/'});assert.equal(link.target,'_blank');assert.equal(link.rel,'noopener noreferrer');assert.match(link.href,/^https:/);assert.ok(link.textContent.includes(payload));});
check('source_validation_and_modular_navigation',()=>{assert.equal(sourceValidation().status,'PASS');for(const route of ['home','evidence','create','review','settings'])assert.ok(read('src/index.html').includes('data-route="'+route+'"'));assert.ok(read('src/app.js').includes('Module boundary · Not implemented'));});
check('upstream_anvil_closure_candidate_isolation_and_context',async()=>{const entries=paths(project).filter(p=>A.editablePath(p)).map(p=>({path:p,bytes:fs.readFileSync(path.join(project,p))}));assert.equal(A.validateProjectClosure(entries).id,'icr-workbench');const baseDigest=await A.digestEntries(entries),contextDigest='a'.repeat(64);const original=read('src/style.css');const proposal={baseDigest,contextDigest,proposalId:'test-only',buildBrief:'Isolated candidate test',operations:[{op:'replace',path:'src/style.css',content:original+'\n/* candidate */\n'}]};const candidate=await A.constructCandidate(proposal,entries,{baseDigest,contextDigest});assert.notEqual(candidate.candidateDigest,baseDigest);assert.equal(read('src/style.css'),original);assert.equal(A.validateProjectClosure(candidate.entries).id,'icr-workbench');await assert.rejects(A.constructCandidate(proposal,entries,{baseDigest:'b'.repeat(64),contextDigest}));const modelContext=capabilityModelContext(project,library);assert.equal(modelContext.implementationSourceIncluded,false);assert.ok(!JSON.stringify(modelContext).includes('const PAYLOAD'));});
check('clean_build_standalone_static_and_repeat_deterministic',async()=>{fs.rmSync(path.join(project,manifest.output),{force:true});const a=await build();const b=await build();assert.equal(a.built.html,b.built.html);assert.ok(a.built.html.includes('window.STICKSHIFT_TOOL'));assert.ok(a.built.html.includes('id="tool-descriptor"'));assert.ok(a.built.html.includes('data-paperclip-capability="icr-evidence-foundation@1.0.1"'));assert.ok(a.built.html.includes(pin.manifest_sha256));assert.ok(!/<(?:script|link|img|iframe)\b[^>]*(?:src|href)\s*=\s*["'](?:https?:)?\/\//i.test(a.built.html));assert.ok(!/\bfetch\s*\(|\bXMLHttpRequest\b|\bWebSocket\s*\(/.test(state.resolved[0].implementation));for(const p of ['src/app.js','assets/foundation/adapter.js','assets/foundation/provider.js','assets/foundation/stores.js','assets/foundation/render.js']){const r=spawnSync(process.execPath,['--check',path.join(project,p)],{encoding:'utf8'});assert.equal(r.status,0,r.stderr);}const scripts=[...a.built.html.matchAll(/<script([^>]*)>([\s\S]*?)<\/script>/g)].filter(m=>!m[1].includes('type='));for(const m of scripts)new vm.Script(m[2]);});
check('ordinary_build_rejects_stale_capability_source',async()=>{const target=path.join(project,'assets/foundation/render.js'),original=fs.readFileSync(target);try{fs.appendFileSync(target,'\n// stale test\n');await assert.rejects(build(),/Stale capability source lock/);}finally{fs.writeFileSync(target,original);}});
after(()=>write(path.join(project,'validation/foundation-tests.json'),{schema_version:'1.0.0',status:result.every(x=>x.status==='PASS')&&result.length===26?'PASS':'FAIL',test_count:result.length,tests:result.sort((a,b)=>a.id.localeCompare(b.id)),browser_dom_execution:false}));
